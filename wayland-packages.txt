# Packages to keep/install for a kiosk Wayland base (labwc + cage + GPU/VAAPI).
# Installed after purge with: apt-get install -y --no-install-recommends
# One package name per line. Lines starting with # and blank lines are ignored.

# Kiosk compositors
labwc
cage
xwayland
libwlroots-0.19

# Wayland libraries
libwayland-client0
libwayland-server0
libwayland-egl1
libwayland-cursor0

# GPU / KMS
mesa-libgallium
mesa-vulkan-drivers
libegl1
libgbm1
libdrm2
libgl1-mesa-dri

# Hardware decode (VAAPI / VDPAU)
libva2
libva-drm2
mesa-va-drivers
va-driver-all
mesa-vdpau-drivers
vdpau-driver-all

# Decode stacks
ffmpeg
gstreamer1.0-plugins-base
gstreamer1.0-plugins-good
gstreamer1.0-plugins-bad
gstreamer1.0-libav
gstreamer1.0-gl

# Kept after purge: required by kiosk install (do not re-add to packages.txt)
# Source: Thorium .deb dependencies (Depends: libasound2, xdg-utils)
libasound2t64
libasound2-data
xdg-utils

# Source: labwc/cage / ffmpeg/gstreamer Wayland stack (hard deps / GTK)
adwaita-icon-theme
hicolor-icon-theme
at-spi2-common
dconf-gsettings-backend
dconf-service
gsettings-desktop-schemas

# Source: ffmpeg/gstreamer video stack (audio libs pulled as deps)
libpulse0
libpipewire-0.3-0t64
libspa-0.2-modules
timgm6mb-soundfont
# Source: gstreamer1.0-plugins-bad (Depends: libldacbt-enc2)
libldacbt-enc2

# Source: system UI/Thorium fallback after font purge
fonts-dejavu-core
# Source: fonts-dejavu-core (Depends: fonts-dejavu-mono)
fonts-dejavu-mono
# Source: Thorium .deb (Depends: fonts-liberation)
fonts-liberation

# Source: Pi 5 boot kernel (do not purge rpi-2712 when removing rpi-v8)
linux-image-rpi-2712
linux-base-rpi-2712

# Source: first-boot rootfs expand (initramfs resize_early + rpi-resize.service).
# Purging pi-bluetooth/desktop metas lets autoremove drop this; reinstall it.
# Hard-deps raspi-config (and thus alsa-utils / dconf-cli); not intentional audio userland.
raspberrypi-sys-mods
# Source: raspi-config (Depends via raspberrypi-sys-mods)
alsa-utils
dconf-cli
